spring:
  datasource:
    url: jdbc:mysql://localhost:3306/connex_db
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  config:
    import: optional:file:.env[.properties]

jwt:
  secret: ${JWT_SECRET_KEY}
  expiration: 3600
